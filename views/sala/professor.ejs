<% include ../partials/headerProfessor %>
<div class="container" >
    <header>
        <div class="row" >
            <div class="col s12"  >
                <h3 class="flow-text" >                    
                    <form class="col s12" action="/sala/<%- exercicio._id %>?_method=put" method="post">
                        <i class="material-icons left" >playlist_play</i>Sala
                        <button class="btn waves-effect waves-light red lighten-1 right" type="submit" name="action">Finalizar
                            <i class="material-icons left">stop_screen_share</i>
                        </button>
                    </form>
                </h3>
            </div>
        </div>
    </header>   
</div>
<script src="/socket.io/socket.io.js" ></script>
<script>
    const socket = io();
    socket.on('send-client', (msg) => {       
        document.getElementById('chat').innerHTML += msg;
    });
    const enviar = () => {      
        socket.emit('send-server', {nome, msg});
    };
</script>
<% include ../partials/footerProfessor %>